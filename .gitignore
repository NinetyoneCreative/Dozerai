/node_modules
/.next
/build
# NOTE: /out (static export) is intentionally COMMITTED so Hostinger can serve
# it from a plain `git pull` without running a build. If you switch to a
# build-on-deploy or CI flow, re-add `/out` here and `git rm -r --cached out`.
*.tsbuildinfo
next-env.d.ts
.env*.local
.DS_Store

# Claude Code local (personal) settings — keep launch.json shared, ignore the rest
.claude/settings.local.json
